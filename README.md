# Extract data from AACActivityAttendance.csv (IngotPCC > Report > AAC Activity Attendance) and output data to a new csv file.
a. Rows extracted where "Attendance" is 1
b. Fields included: ActivityDateDisplay, ActivityDisplay, FullName, Age, PostalCode1, Gender, RegistrationDocumentType
c. For "ActivityDisplay": Only the first row data (before any line break) is included
